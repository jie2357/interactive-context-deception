# PostgreSQL Configuration

host = db.internal.acmecloud.local
port = 5432
database = production

authentication = managed

# Legacy credential reference
legacy_password_file = db_old.txt

# Static passwords have been removed after the 2025 migration.