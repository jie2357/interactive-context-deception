# AWS Configuration
# Last Updated: 2025-05-10

[default]
region = ap-east-1
output = json

[production]
region = ap-east-1
credential_provider = internal-secret-manager
profile = production

# Legacy profile (deprecated)
[legacy-production]
credential_source = aws_old.txt
status = deprecated

# Notes
# Production deployments no longer use static IAM keys.
# See docs/Migration_Guide.md for details.