# Example Environment File

APP_ENV=production

AWS_PROFILE=production

DATABASE_HOST=db.internal.acmecloud.local

DATABASE_PORT=5432

LOG_LEVEL=INFO

# Production credentials are injected during deployment.
#
# Do NOT store secrets inside this repository.